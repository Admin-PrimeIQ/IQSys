HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified


# Environment
NODE_ENV=development

# Database - AWS RDS MySQL
DATABASE_CLIENT=mysql
DATABASE_HOST=strapi.cfkkoyk00gnr.us-east-2.rds.amazonaws.com
DATABASE_PORT=3306
DATABASE_NAME=strapi
DATABASE_USERNAME=admin
DATABASE_PASSWORD=Aurora2Real
DATABASE_SSL=true

# Server
HOST=0.0.0.0
PORT=1337

# App Keys (generar claves únicas para producción)
APP_KEYS="toBeModified1,toBeModified2"

# API Token Salt (generar salt único para producción)
API_TOKEN_SALT=tobemodified

# Admin JWT Secret (generar secret único para producción)
ADMIN_JWT_SECRET=tobemodified

# Transfer Token Salt (generar salt único para producción)
TRANSFER_TOKEN_SALT=tobemodified

# JWT Secret (generar secret único para producción)
JWT_SECRET=tobemodified